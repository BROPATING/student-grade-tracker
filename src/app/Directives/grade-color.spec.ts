import { GradeColor } from './grade-color';

describe('GradeColor', () => {
  it('should create an instance', () => {
    const directive = new GradeColor();
    expect(directive).toBeTruthy();
  });
});
